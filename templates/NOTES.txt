Red Hat Offline Knowledge Portal has been deployed as "{{ include "rhokp.fullname" . }}".

{{- if .Values.route.enabled }}
Route:
  oc get route {{ include "rhokp.fullname" . }} -n {{ .Release.Namespace }}
{{- else }}
No Route enabled. Access the service internally or port-forward:
  kubectl port-forward svc/{{ include "rhokp.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
{{- end }}

{{- if not .Values.accessKey.secretValue }}
WARNING: accessKey.secretValue is not set — the pod will fail to start without
an ACCESS_KEY secret. Set accessKey.secretValue (or pre-create a Secret named
{{ .Values.accessKey.secretName }} yourself).
{{- end }}

{{- if not .Values.imagePullSecret.dockerPassword }}
WARNING: imagePullSecret.dockerPassword is not set — image pulls from
{{ .Values.imagePullSecret.dockerServer }} will fail (or pre-create a Secret named
{{ .Values.imagePullSecret.name }} yourself).
{{- end }}
