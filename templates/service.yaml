apiVersion: v1
kind: Service
metadata:
  name: {{ include "rhokp.fullname" . }}
  labels:
    {{- include "rhokp.selectorLabels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "rhokp.selectorLabels" . | nindent 4 }}
